/// @description Insert description here
// You can write your code in this editor
message_length = string_length(full_text);


if (cutoff < message_length)
{
    if (counter == 0)
    {
        cutoff += text_speed; 
    }
}

scr_text_counter_overworld();